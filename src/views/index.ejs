<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Document</title>
		<link rel="stylesheet" href="/styles.css">
	</head>
	<body>
		<%- include('navbar',{links}) %>
		<h1><%=/*Note: This can be accessed as locals.message or message */ message %></h1>
		<ul>
			<% users.forEach((user)=>{ %>
				<%- include('users',{user}) %>
			<% }); %>
		</ul>
	</body>
</html>
